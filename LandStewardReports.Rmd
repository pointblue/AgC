---
title: |
  <div style="display: flex; align-items: center; gap: 150px;">
    <img src="ReportRef/Images/PBLogo.png" style="width: 300px;">
    <img src="ReportRef/Images/AgCLogo.svg" style="width: 300px;">
  </div>
  Land Steward Report
author: "`r params$author`"
date: "`r format(Sys.Date(), '%B %d %Y')`"
output:
  bookdown::html_document2:
    number_sections: FALSE
    toc: true
    toc_float: true
    toc_depth: 3  # Include up to level 3 headers
    self_contained: true
    fig_width: 9
    fig_height: 6
dev: png
params:
  project_name: JEME.24.PG
  raca_filter:
    - range
  author: "Avalon Cook, Lisa Eash, Chloe Latt, and Erika Foster"
  stocks: FALSE
---
```{r Setup, echo=FALSE, message=FALSE, warning=FALSE, error=FALSE, results='hide'}
#Created by: Avalon Cook
#Created: 20231214
#Updated: 20241126 (by AC)

#test change

source('packages.R')
source('functions.R')

#renv::restore() #CHECK add renv to this rproj?
data_path<-("Z:/Soils Team/AgC Data/Master Datasheets")

knitr::opts_chunk$set(
	echo = FALSE,
	message = FALSE,
	warning = FALSE, 
	error=FALSE)
```
```{r, project.summary}
#pulling in spatial data
spatial_path <- "Z:/Soils Team/AgC Data/Raw Data/Spatial Data/ZippedShapefiles" #define the location of ACTION GIS data

border_pattern <- paste0(params$project_name, "_border.zip") #specify the pattern of the border file
borderzip_file <- list.files(
  path = spatial_path,
  pattern = border_pattern,
  recursive = TRUE,
  full.names = TRUE) # Search for the matching file, recursively
temp_folder1 <- tempfile() #create a temp directory where the shapefile can be unzipped
unzip(borderzip_file, exdir = temp_folder1)
border_file<-list.files(temp_folder1, pattern = "\\_border.shp$", full.names = TRUE)
border<- read_sf(border_file) #read in project polygon

#repeat for sampling points
points_pattern <- paste0(params$project_name, "_pointsfinal.zip") 
pointszip_file <- list.files(
  path = spatial_path,
  pattern = points_pattern,
  recursive = TRUE,
  full.names = TRUE) 
temp_folder2 <- tempfile() 
unzip(pointszip_file, exdir = temp_folder2)
points_file<-list.files(temp_folder2, pattern = "\\_pointsfinal.shp$", full.names = TRUE)
points<- read_sf(points_file) 

#Reading in master datasheets and filtering by project of interest
list_dfs_field<-list.files(file.path(data_path, "FieldLevel", ""), pattern = "\\.csv$", full.names = TRUE)
FieldLevel<-list_dfs_field[which.max(as.Date(gsub("\\D","", list_dfs_field), format = "%Y%m%d"))]%>%
  read.csv()%>%
  filter(project_id == params$project_name)

list_dfs_point<-list.files(file.path(data_path, "PointLevel", ""), pattern = "\\.csv$", full.names = TRUE)
PointLevel<-list_dfs_point[which.max(as.Date(gsub("\\D","", list_dfs_point), format = "%Y%m%d"))]%>%
  read.csv(na.strings="")%>%
  filter(project_id == params$project_name)%>%
  mutate(plot_type = substr(sample_id, 12, 12)) #add the plot_type column

list_dfs_design<-list.files(file.path(data_path, "ProjectDesign", ""), pattern = "\\.csv$", full.names = TRUE)
ProjectDesign<-list_dfs_design[which.max(as.Date(gsub("\\D","", list_dfs_design), format = "%Y%m%d"))]%>%
  read.csv()%>%
  filter(project_id == params$project_name)

#add project design datasheet, then assign all the variables created above

#Establishing variables for report structure
proj.protocol <- FieldLevel$protocol[1]
proj.inscore <- ProjectDesign$inference_score[1]
proj.propertyname <- ProjectDesign$ranch_name[1]
proj.implementationyear <- if(is.na(ProjectDesign$proj_practice_year[1])){""}
pract_codes<-read.csv('practice_abbreviations.csv')
practs_vec<-strsplit(ProjectDesign$conservation_practice[1], ",\\s*")[[1]]
practs_full<-pract_codes$Practice[match(practs_vec, pract_codes$Abbreviation)]
proj.practicetype <- format_list(practs_full)
proj.title <- paste0(proj.propertyname, ": ", proj.implementationyear, proj.practicetype)

#Defining project indicators
indicator.db<-read.csv("ReportRef/indicator_descriptions.csv", check.names=FALSE)#read in the indicator descriptions reference sheet
proj.indicator.table<-indicator.db%>% 
  rowwise() %>%
  filter(!all(is.na(PointLevel[[CheckCol]])))%>%
  filter(!Acronym %in% c("Sand", "Silt", "Clay"))%>%
  filter(Acronym!="N") #CHECK! Catie asked me to remove total N as producers will confuse with nitrates
proj.indicators<-proj.indicator.table$Acronym #Get a vector of indicator acronyms for this project to use for further logic
if ("Tx" %in% proj.indicators) { #create a vector of value names that match the names in PointLevel (replacing Tx with sand silt and clay)
  proj.indicators.SSC<-c(proj.indicator.table$CheckCol, "sand", "silt")
} else {proj.indicators.SSC <- proj.indicator.table$CheckCol}
```

# `r proj.propertyname` {.unlisted .unnumbered}
#### Project Code: `r FieldLevel$project_id[1]` {.unlisted .unnumbered}
#### Conservation practice(s): `r proj.practicetype` {.unlisted .unnumbered}

## Background
Thank you for participating in the Agricultural Conservation “Ag-C” Monitoring Program, developed to evaluate ecosystem impacts of common conservation practices on farms and ranches. The Ag-C program supports transparent, fit-for-purpose monitoring of multiple indicators and focuses on carbon as a primary indicator of ecosystem health. Ag-C monitoring tracks management impacts at the farm scale and also aggregates data to evaluate management effects at regional scales. With specified data-sharing permission, the collected information, from management history to field measurements, can support greater understanding of commonly incentivized practice impact on ecosystem carbon.

This report reviews the values for the carbon indicators measured at your agricultural site. Over time, continued use of the Ag-C protocols will allow assessment of your conservation management impacts. Find additional information and updates about the Ag-C Monitoring Program on [our landing page](https://www.pointblue.org/ag-c/) or sign up for important email updates [here](https://forms.office.com/r/TANxB68bAb). 

## Project Design
The Ag-C program offers a flexible monitoring approach, with options to select indicators of interest, tailor methods to your goals and resources, and match project design elements to your context. Ag-C is made of two major protocols: Range-C to accommodate monitoring in rangeland systems and Crop-C to accommodate monitoring in cropland systems. At your site, `r proj.protocol` was used to create a monitoring plan.^[To learn more about designing a monitoring program with `r proj.protocol`, consult the [`r proj.protocol` Handbook](`r ifelse(proj.protocol=='Range-C', 'https://www.pointblue.org/?sdm_process_download=1&download_id=19991', 'https://www.pointblue.org/?sdm_process_download=1&download_id=20943')`)]

To support interpretability and comparability, `r proj.protocol` assigns each monitoring project an “inference score” which reflects how decisions made in the project design process impact the overall rigor and reliability of the resulting data. Inference scores range from 40 to 100, where the lowest score meets rigor and reliability standards to achieve target outcomes and the highest score reflects academic, research-level confidence in the project’s findings and/or a more holistic characterization of carbon dynamics. 

### Your inference score

The monitoring plan developed for the `r paste0(proj.implementationyear, " ", proj.practicetype)` practice at `r proj.propertyname ` results in a `r proj.protocol` **inference score of** **`r proj.inscore`**. In addition to the indicators measured, their sampling densities, and the selected methods, the following design factors contributed to the score: 

```{r Dynamic text Baseline and Control, results='asis'}
if (ProjectDesign$control_baseline=="TRUE" & length(unique(border$plot_type))==2){cat(paste0("- This ", proj.protocol, " project includes baseline monitoring before the conservation practice was implemented as well as parallel monitoring at an untreated control site.^[The most rigorous project design includes baseline monitoring before the conservation practice is implemented, as well as parallel monitoring at an untreated control site into the future. Although not logistically possible for every project, these elements together provide highly interpretable results.]", "<br>"))}

if (ProjectDesign$control_baseline=="TRUE" & length(unique(border$plot_type))==1){cat(paste0("- This ", proj.protocol, " project includes baseline monitoring before the conservation practice was implemented, but did not include parallel monitoring at an untreated control site.^[The most rigorous project design includes baseline monitoring before the conservation practice is implemented, as well as parallel monitoring at an untreated control site into the future. Although not logistically possible for every project, these elements together provide highly interpretable results.]", "<br>"))}

if (ProjectDesign$control_baseline=="FALSE" & length(unique(border$plot_type))==2){cat(paste0("- This ", proj.protocol, " project includes parallel monitoring at an untreated control site, but did not capture baseline conditions before practice implementation.^[The most rigorous project design includes baseline monitoring before the conservation practice is implemented, as well as parallel monitoring at an untreated control site into the future. Although not logistically possible for every project, these elements together provide highly interpretable results.]", "<br>"))}
  
if (ProjectDesign$control_baseline=="FALSE" & length(unique(border$plot_type))==1){cat(paste0("- This ", proj.protocol, " project did not capture baseline conditions before practice implementation and was not able to compare current conditions to an untreated control site. While we are still able to characterize current conditions now and into the future, the results will have limited interpretability for understanding the full scope of practice impact.^[The most rigorous project design includes baseline monitoring before the conservation practice is implemented, as well as parallel monitoring at an untreated control site into the future. Although not logistically possible for every project, these elements together provide highly interpretable results.]", "<br>"))}
```
  
```{r, max depth and selection method, results = 'asis'}
depths.inches<-unique(PointLevel$target_depth)%>%convert_depths()
max.depth.inches <- max(as.numeric(sub(".*-", "", depths.inches)))
max.depth.cent <- max(as.numeric(sub(".*-", "", unique(PointLevel$target_depth))))

cat(paste0("- Soil samples were taken to a target maximum depth of ", max.depth.inches, " inches.\n\n"))

if (ProjectDesign$point_generation_method=="gis"){
  cat("- Sampling points were randomly selected within the monitoring boundary using mapping software and located in the field by GPS.")
}else{cat("- Sampling points were randomly selected using the Range-C Point Selector tool and paced out in the field.")}
```


### Indicators
The following table lists carbon and other indicators that were included in this monitoring project and their corresponding acronyms that will be used in the following tables, graphs, and maps. A description of each indicator and reported units are provided. 
```{r, , results='asis'}
if ("Extra" %in% proj.indicator.table$Status){
cat(paste0("Note that your project includes indicators that aren’t directly related to carbon dynamics. These indicators provide additional information about site dynamics but aren’t covered in the ", proj.protocol,  " handbook."))}
```
```{r}
source("ReportRef/IndicatorTable_prep.R")
IndicatorTableFinal
```

### Spatial design
The interactive map below shows the boundaries of the study area for monitoring and how sampling points are distributed. 
```{r}
#spatial data was pulled in above. Use this chunk to render your map.
source("ReportRef/SpatialDeisgnMap_prep.R")
SpatialDesignMapFinal
```
```{r Dynamic text generation 1, results='asis'}
#this section states which, if any, indicators were measured at every sampling point and removes them from the following legend table

#Start with a filtering step so we're only comparing among the first timepoint, otherwise things will get weird as we often don't measure certain indicators more than once
PointLevel_firstT<- PointLevel %>%
  mutate(time_suffix = as.numeric(str_sub(timepoint, -1))) %>%
  filter(time_suffix == min(time_suffix, na.rm = TRUE)) %>%
  select(-time_suffix)

allpoints.list <- proj.indicator.table$CheckCol[
  proj.indicator.table$CheckCol %in% names(PointLevel_firstT) &    # must exist
    sapply(proj.indicator.table$CheckCol, function(col) all(!is.na(PointLevel_firstT[[col]])))
]

allpoints.table <- proj.indicator.table %>%
  filter(CheckCol %in% allpoints.list)

```
```{r Dynamic text generation 2, indicator.legend, results='asis'}
if (identical(allpoints.list, proj.indicator.table$CheckCol)){ #all indicators measured at every point
  cat("All indicators listed in Table 1 were measured at each sampling point shown in the map.")
}


if (identical(allpoints.list, proj.indicator.table$CheckCol)==FALSE){ #only proceed if some indicators were not measured at every point
  
  #start by listing the indicators that were measured everywhere
  cat("The following indicators were measured at every sampling point:", paste(allpoints.table$Indicator, collapse = ", "), "<br><br>")
  
  #identifying indicators that were measured at a subset of points
  subset.inds <- proj.indicator.table$CheckCol[ 
  proj.indicator.table$CheckCol %in% names(PointLevel) &    # must exist
    sapply(proj.indicator.table$CheckCol, function(col) any(is.na(PointLevel[[col]])))] 
  
  #checking if all those subset indicators were done at the same points -- if so we'll just list them instead of making a table
  if (all(apply(!is.na(PointLevel[subset.inds]), 1, function(x) all(x) | all(!x)))){ #returns TRUE if all subset indicators were measured at the same sampling points
    subset_points <- PointLevel$sample_id[complete.cases(PointLevel[subset.inds])]
    subset.inds.name<-proj.indicator.table$Indicator[proj.indicator.table$CheckCol %in% subset.inds]
    cat(paste0("The following indicators were measured at points ", format_list(substr(subset_points, 12, 15)), ": ", format_list(subset.inds.name),"<br><br>"))
  }
  
  #CHECK Here is where we go back to generating the table for when not all subset inds were measured at the same point. Not relevant for current report deadline. Model based on pilot reports code when ready
  if (all(apply(!is.na(PointLevel[subset.inds]), 1, function(x) all(x) | all(!x)))==FALSE){ 
    cat("Indicators that were only measured at some sampling points are listed in the table below.")
    
    #might have to generate the table in a different chunk, don't remember
  }
  
}

```
```{r, other spatial design elements, results='asis'}
if (ProjectDesign$composite==TRUE){
  cat("Each soil sample was composed of smaller composited samples to capture small-scale variability at each sampling point. ")
}



#CHECK add statement for stratification? Not worried about it right now but may come back after deadline
```
```{r, depth increments, results='asis'}
#Do some conversions for a statement about max depth and depth increments
depth.increments.no <- ProjectDesign$soc_depth_increments


if (depth.increments.no>1){
  cat("Depth increments from ", depths.string[1], " inches and ", depths.string[2], " inches were analyzed separately.")
  
  depth_sentence(depths.inches)
  
}
```
### Timepoints
```{r}
timepoint_dates<-PointLevel %>%
  group_by(timepoint) %>%
  summarize(sample_date = first(sample_date), .groups = "drop")%>%
  arrange(timepoint) #make sure timpoints are in order for indexing

timepoint_sentence <- timepoint_dates %>%
  mutate(
    text = paste0(
      timepoint,
      " was sampled on ",
      format(as.Date(sample_date, format = "%m/%d/%Y"), "%B %d, %Y"),
      "."
    )
  ) %>%
  pull(text) %>%          # get vector of strings
  paste(collapse = " ")

#setting up a table for ggplot labels
tp_lookup<- timepoint_dates %>%
  mutate(
    sample_date = as.Date(sample_date, format="%Y-%m-%d %H:%M:%S"),
    year_label = format(sample_date, "%Y")
  )

```
This monitoring project includes `r length(unique(PointLevel$timepoint))` timepoint(s) of data. `r timepoint_sentence` Continued monitoring can provide additional insight into the long-term impact of your conservation practice(s). 
```{r, baseline statements, results='asis'}
if ((length(unique(PointLevel$timepoint)) == 1) && (unique(PointLevel$timepoint) == "T0")){
  cat("\\n Note that results shown in this report are for the **baseline timepoint only**. This means that these results are a starting point for assessing conditions prior to management change and no impact from conservation practices is shown.")
}

#separate statement for projects with no baseline or time series
if ((length(unique(PointLevel$timepoint)) == 1 && unique(PointLevel$timepoint)=="T1")){
    cat("\\n Note that monitoring for this site began after conservation practices were implemented. Over time, continued monitoring may show changes due to conservation, but the full impact of the practice(s) won't be captured. ")
}
#And another for no baseline with time series
if ((length(unique(PointLevel$timepoint)) > 1 & "T0" %in% PointLevel$timepoint == FALSE)){
    cat("\\n Note that monitoring for this site began after conservation practices were implemented. Repeated monitoring may reveal continued changes since implementation, but the full impact of the practice(s) is not captured by these results.")
}
```
```{r, remonitoring interval statement, results='asis'}
if (proj.protocol == "Crop-C"){
rec_interval <- min(pract_codes$Interval_Crop[match(practs_vec, pract_codes$Abbreviation)])
}else {
  rec_interval <- min(pract_codes$Interval_Range[match(practs_vec, pract_codes$Abbreviation)])
}

if (proj.protocol == "Crop-C"){
impact_pract <- (pract_codes[match(practs_vec, pract_codes$Abbreviation),]%>%arrange(Interval_Crop))[1,1]
}else {
  impact_pract <- (pract_codes[match(practs_vec, pract_codes$Abbreviation),]%>%arrange(Interval_Range))[1,1]
}


if (length(unique(PointLevel$timepoint))>1){

timediff<-tp_lookup$sample_date[length(tp_lookup$sample_date)] - tp_lookup$sample_date[1]
current_interval<-round(as.numeric(timediff)/365) 

cat("\\n We expect to see increased soil organic carbon from your conservation practice ", rec_interval, " year(s) after implementation of the most impactful practice at your site: ", impact_pract,". The first and last monitoring timepoint for this project are ", current_interval, " year(s) apart.", sep = "")

if (current_interval >= rec_interval){
  cat(" We expect this timeframe to be sufficient for detecting change.")
} else {
  cat("**This monitoring timeframe may be too soon to detect change from the conservation practice.**")
}

}

if (length(unique(PointLevel$timepoint))==1 && (unique(PointLevel$timepoint) == "T0")){

cat("\\n We expect to see increased soil organic carbon from your conservation practice ", rec_interval, " year(s) after implementation of the most impactful practice at your site: ", impact_pract,".", sep = "") 
  
}

```

## Results
```{r, raca data prep, include = FALSE}
source("ReportRef/RaCAData_prep.R")
```
```{r, results='asis'}
cat("The following graph(s) compare data collected at your site to other agricultural sites in your ecoregion: ", 
       ecoregion,
       ". These comparison values are from the [USDA’s Rapid Carbon (RaCA) Assessment dataset](https://www.nrcs.usda.gov/resources/data-and-reports/rapid-carbon-assessment-raca), and provide context for understanding how your site stacks up against other farms in comparable climates. As you continue implementing conservation practices, your soil health indicators have the potential to improve, moving closer to the more ideal values seen in your region. The data from the RaCA program was collected in 2010, so shifts in climate and other conditions since the comparison dataset was sampled should be taken into consideration.", sep=""

)
```

#### ~Prepping results data{.hidden}
```{r, include=FALSE}
#This will only evaluate if there is bulk density AND  the user opted to show stocks in the report
if ("bulk_density" %in% proj.indicators.SSC && params$stocks){
  source("ReportRef/StocksData_prep.R")
}

#There will always be SOC% data, no condition needed
source("ReportRef/SOCData_prep.R")

#only runs if BD is included
if("bulk_density" %in% proj.indicators.SSC){
  source("ReportRef/BDData_prep.R")
}

#Contains internal conditions for stocks vs non-stocks
source("ReportRef/MeansData_prep.R")

#prep color palettes
source("ReportRef/Palette_prep.R")

#prep PLFA data if needed
if (all(c("bact_bio", "fung_bio") %in% proj.indicators.SSC)) {
source("ReportRef/PLFAData_prep.R")
}
```
#### SOC% Results {.hidden}
```{r results='asis', eval = !("bulk_density" %in% proj.indicators.SSC) || !params$stocks}
#This will only evaluate if there is no bulk density or if the user opted not to show stocks in the report
asis_output("### Soil organic carbon concentrations\\n") #CONDITIONAL HEADER
```
```{r eval = !("bulk_density" %in% proj.indicators.SSC) || !params$stocks, results = 'asis', warning=FALSE}
#This will only evaluate if there is no bulk density or if the user opted not to show stocks in the report

source("ReportRef/SummaryStats_SOC.R")


if ( length(unique(PointLevel$timepoint))==1 & length(unique(PointLevel$plot_type))==1 ){
  cat(percentile_scentence_soc)
}

if ( length(unique(PointLevel$timepoint))>1 & length(unique(PointLevel$plot_type))==1 ){
  cat(percentile_scentence_soc, "\n\n")
  cat(time_change_soc)
}

if ( length(unique(PointLevel$timepoint))==1 & length(unique(PointLevel$plot_type))>1 ){
  cat(percentile_scentence_soc, "\n\n")
  cat(treatment_contrast_soc)
}

if ( length(unique(PointLevel$timepoint))>1 & length(unique(PointLevel$plot_type))>1 ){
  cat(percentile_scentence_soc, "\n\n")
  cat(baci_soc)
}
```
```{r eval = !("bulk_density" %in% proj.indicators.SSC) || !params$stock, fig.cap="Average soil organic carbon content. The error bars around average values show the range of confidence based on data variability (95% confidence intervals). Data collected at your site are compared to USDA Rapid Carbon Assessment values for other agricultural lands in your ecoregion."}
#This will only evaluate if there is no bulk density or if the user opted not to show stocks in the report
source("ReportRef/SOCfig_prep.R")
SOCfig
```
``` {r results='asis', eval = !all(is.na(PointLevel$position))}
cat("Background points showing unique sample values are color-coded according to whether they fell in the crop row or alley. Refer to the table below for a breakdown of plot averages by field position.")

soc_by_position<-soc_df%>%
  filter(plot_type %in% c("T", "C"))%>%
  group_by(plot_type, timepoint, LU)%>%
  summarise(org_c = mean(org_c, na.rm = TRUE), .groups = "drop")%>%
  mutate(
    plot_type = factor(plot_type, levels = c("T", "C")),
    LU        = factor(LU, levels = sort(unique(LU))),
    timepoint = factor(timepoint, levels = tp_lookup$timepoint)
  )%>%
  arrange(plot_type, timepoint, LU)#this order is very important and corresponds to the order of the headers

  position_wider<-soc_by_position%>%pivot_wider(
    names_from = c("plot_type", "LU", "timepoint"),
    values_from = org_c,
    names_sep = ","
  )

# setup dynamic headers
plot_types <- unique(soc_by_position$plot_type)
LUs <- unique(soc_by_position$LU)
timepoints <- unique(soc_by_position$timepoint)

#switch timepoints to the year labels
timepoints<- tp_lookup$year_label[
  match(as.character(timepoints), tp_lookup$timepoint)
]

LU_header<-rep(LUs, times = length(timepoints)*length(plot_types))


timepoints_header<-setNames(rep(length(LUs), times=length(rep(timepoints, times=length(plot_types)))), rep(timepoints, times=length(plot_types)))
plot_header<-setNames(rep(length(timepoints_header), length(plot_types)),plot_types%>%recode(`T`="Treated plot", C = "Control plot"))

#create table with nested headers
position_wider%>%
  kbl(
    caption = "Mean SOC% by field position",
    digits=2,
    col.names = LU_header,
    align     = "c" 
  )%>%
  { 
    if (length(timepoints) > 1) {
      add_header_above(., timepoints_header, align     = "c")
    } else {
      .
    }
  } %>%
  add_header_above(plot_header, align     = "c")%>%
  kable_styling(position = "center", full_width = TRUE)

```

#### PLFA Results {.hidden}
```{r results='asis', eval = (all(c("fung_bio", "bact_bio") %in% proj.indicators.SSC))}
#This will only evaluate if there is no bulk density or if the user opted not to show stocks in the report
asis_output("### Phospholipid Fatty Acid Results\\n") #CONDITIONAL HEADER

cat("PLFA (phospholipid fatty acid) analysis measures the microbial community in soil by analyzing signature molecules from bacterial, fungal, and other microbes. It provides insights into community composition and soil biological activity, which are key indicators of soil health. PLFA data can be analyzed in several ways, but looking at combined microbial biomass and fungal to bacterial ratios is common. Higher microbial biomass is associated with greater microbial activity and nutrient cycling, while a higher fungal to bacterial ratio is associated with greater organic matter stabilization and improved soil structure.")
```

```{r eval = (all(c("fung_bio", "bact_bio") %in% proj.indicators.SSC)), results = 'asis'}
#This will only evaluate if there is no bulk density or if the user opted not to show stocks in the report

#source("ReportRef/SummaryStats_PLFA.R")

#UPDATE#
#if ( length(unique(PointLevel$timepoint))==1 & length(unique(PointLevel$plot_type))==1 ){
#  cat(percentile_scentence_soc)
#}

#if ( length(unique(PointLevel$timepoint))>1 & length(unique(PointLevel$plot_type))==1 ){
#  cat(percentile_scentence_soc, "\n\n")
#  cat(time_change_soc)
#}

#if ( length(unique(PointLevel$timepoint))==1 & length(unique(PointLevel$plot_type))>1 ){
#  cat(percentile_scentence_soc, "\n\n")
#  cat(treatment_contrast_soc)
#}

#if ( length(unique(PointLevel$timepoint))>1 & length(unique(PointLevel$plot_type))>1 ){
#  cat(percentile_scentence_soc, "\n\n")
#  cat(baci_soc)
#}
```
```{r eval = (all(c("fung_bio", "bact_bio") %in% proj.indicators.SSC)), fig.cap="Average combined microbial biomass. The error bars around average values show the range of confidence based on data variability (95% confidence intervals)."}
#This will only evaluate if there is no bulk density or if the user opted not to show stocks in the report
source("ReportRef/PLFAfig_prep.R")
PLFAfig_biomass
```
```{r eval = (all(c("fung_bio", "bact_bio") %in% proj.indicators.SSC)), fig.cap="Average fungal to bacterial ratio. The error bars around average values show the range of confidence based on data variability (95% confidence intervals)."}
PLFAfig_ratio
```

#### SOC Stocks Results {.hidden}
```{r results='asis', eval = "bulk_density" %in% proj.indicators.SSC && params$stocks}
#CHECK! Here's the eval statement if you want to re-activate this chunk: "bulk_density" %in% proj.indicators.SSC && params$stocks

#This will only evaluate if there is bulk density AND  the user opted to show stocks in the report

asis_output("### Soil organic carbon stocks\\n") #CONDITIONAL HEADER
```
```{r eval = !("bulk_density" %in% proj.indicators.SSC) || params$stocks, results = 'asis'}
#This will only evaluate if there is bulk density and if the user opted to show stocks in the report

source("ReportRef/SummaryStats_stocks.R")


if ( length(unique(PointLevel$timepoint))==1 & length(unique(PointLevel$plot_type))==1 ){
  cat(percentile_scentence_stocks)
}

if ( length(unique(PointLevel$timepoint))>1 & length(unique(PointLevel$plot_type))==1 ){
  cat(percentile_scentence_stocks, "\n\n")
  cat(time_change_stocks)
}

if ( length(unique(PointLevel$timepoint))==1 & length(unique(PointLevel$plot_type))>1 ){
  cat(percentile_scentence_stocks, "\n\n")
  cat(treatment_contrast_stocks)
}

if ( length(unique(PointLevel$timepoint))>1 & length(unique(PointLevel$plot_type))>1 ){
  cat(percentile_scentence_stocks, "\n\n")
  cat(baci_stocks)
}


```

```{r warning=FALSE, fig.cap="Average soil organic carbon stocks. The error bars around average values show the range of confidence based on data variability (95% confidence intervals). Data collected at your site are compared to USDA Rapid Carbon Assessment values for other agricultural lands in your ecoregion. Points in the background show individual sampling point values.", eval = "bulk_density" %in% proj.indicators.SSC && params$stocks}
#CHECK! Here's the eval statement if you want to re-activate this chunk: "bulk_density" %in% proj.indicators.SSC && params$stocks
#This will only evaluate if there is bulk density AND  the user opted to show stocks in the report
source("ReportRef/Stocksfig_prep.R")
Stocksfig
```
``` {r results='asis', eval = !all(is.na(PointLevel$position)) && params$stocks}
cat("Background points showing unique sample values are color-coded according to whether they fell in the crop row or alley. Refer to the table below for a breakdown of plot averages by field position.")

stocks_by_position<-SOC_stocks_df%>%
  filter(plot_type %in% c("T", "C"))%>%
  group_by(plot_type, timepoint, LU)%>%
  summarise(stocks = mean(Tons.Acre, na.rm = TRUE), .groups = "drop")%>%
  mutate(
    plot_type = factor(plot_type, levels = c("T", "C")),
    LU        = factor(LU, levels = sort(unique(LU))),
    timepoint = factor(timepoint, levels = tp_lookup$timepoint)
  )%>%
  arrange(plot_type, timepoint, LU)#this order is very important and corresponds to the order of the headers

  position_wider<-stocks_by_position%>%pivot_wider(
    names_from = c("plot_type", "LU", "timepoint"),
    values_from = stocks,
    names_sep = ","
  )

# setup dynamic headers
plot_types <- unique(stocks_by_position$plot_type)
LUs <- unique(stocks_by_position$LU)
timepoints <- unique(stocks_by_position$timepoint)

#switch timepoints to the year labels
timepoints<- tp_lookup$year_label[
  match(as.character(timepoints), tp_lookup$timepoint)
]

LU_header<-rep(LUs, times = length(timepoints)*length(plot_types))


timepoints_header<-setNames(rep(length(LUs), times=length(rep(timepoints, times=length(plot_types)))), rep(timepoints, times=length(plot_types)))
plot_header<-setNames(rep(length(timepoints_header), length(plot_types)),plot_types%>%recode(`T`="Treated plot", C = "Control plot"))

#create table with nested headers
position_wider%>%
  kbl(
    caption = "Mean SOC stocks by field position in tons per acre",
    digits=2,
    col.names = LU_header,
     align     = "c" 
  )%>%
  { 
    if (length(timepoints) > 1) {
      add_header_above(., timepoints_header,  align     = "c" )
    } else {
      .
    }
  } %>%
  add_header_above(plot_header,  align     = "c" )%>%
  kable_styling(position = "center", full_width = TRUE)
```
#### BD Results {.hidden}
```{r results='asis', eval = ("bulk_density" %in% proj.indicators.SSC)}
#This will only evaluate if there is bulk density
asis_output("### Soil bulk density\\n") #CONDITIONAL HEADER
```
```{r eval = ("bulk_density" %in% proj.indicators.SSC), results = 'asis'}
#This will only evaluate if there is bulk density 

source("ReportRef/SummaryStats_BD.R")

if ( length(unique(PointLevel$timepoint))==1 & length(unique(PointLevel$plot_type))==1 ){
  cat(percentile_scentence_bd)
}

if ( length(unique(PointLevel$timepoint))>1 & length(unique(PointLevel$plot_type))==1 ){
  cat(percentile_scentence_bd, "\n\n")
  cat(time_change_bd)
}

if ( length(unique(PointLevel$timepoint))==1 & length(unique(PointLevel$plot_type))>1 ){
  cat(percentile_scentence_bd, "\n\n")
  cat(treatment_contrast_bd)
}

if ( length(unique(PointLevel$timepoint))>1 & length(unique(PointLevel$plot_type))>1 ){
  cat(percentile_scentence_bd, "\n\n")
  #cat(time_contrasts_bd, "\n\n")
  cat(baci_bd)
}

```
```{r eval = ("bulk_density" %in% proj.indicators.SSC), fig.cap="Average soil bulk density. The error bars around average values show the range of confidence based on data variability (95% confidence intervals). Data collected at your site are compared to USDA Rapid Carbon Assessment values for other agricultural lands in your ecoregion. Points in the background show individual sampling point values. Colored bands in the background show how these values compare to thresholds that apply to USDA recommended bulk density targets for the average soil texture measured at your site."}
#This will only evaluate if there is bulk density AND  the user opted not to show stocks in the report
source("ReportRef/BDfig_prep.R")
BDfig
```
``` {r results='asis', eval = !all(is.na(PointLevel$position))}
cat("Background points showing unique sample values are color-coded according to whether they fell in the crop row or alley. Refer to the table below for a breakdown of plot averages by field position.")

BD_by_position<-bd_df%>%
  filter(plot_type %in% c("T", "C"))%>%
  group_by(plot_type, timepoint, LU)%>%
  summarise(bulk_density = mean(bulk_density, na.rm = TRUE), .groups = "drop")%>%
  mutate(
    plot_type = factor(plot_type, levels = c("T", "C")),
    LU        = factor(LU, levels = sort(unique(LU))),
    timepoint = factor(timepoint, levels = tp_lookup$timepoint)
  )%>%
  arrange(plot_type, timepoint, LU)#this order is very important and corresponds to the order of the headers

  position_wider<-BD_by_position%>%pivot_wider(
    names_from = c("plot_type", "LU", "timepoint"),
    values_from = bulk_density,
    names_sep = ","
  )

# setup dynamic headers
plot_types <- unique(BD_by_position$plot_type)
LUs <- unique(BD_by_position$LU)
timepoints <- unique(BD_by_position$timepoint)

#switch timepoints to the year labels
timepoints<- tp_lookup$year_label[
  match(as.character(timepoints), tp_lookup$timepoint)
]

LU_header<-rep(LUs, times = length(timepoints)*length(plot_types))


timepoints_header<-setNames(rep(length(LUs), times=length(rep(timepoints, times=length(plot_types)))), rep(timepoints, times=length(plot_types)))
plot_header<-setNames(rep(length(timepoints_header), length(plot_types)),plot_types%>%recode(`T`="Treated plot", C = "Control plot"))

#create table with nested headers
position_wider%>%
  kbl(
    caption = "Mean bulk density by field position in g/cm3",
    digits=2,
    col.names = LU_header,
    align     = "c" 
  )%>%
  { 
    if (length(timepoints) > 1) {
      add_header_above(., timepoints_header,  align     = "c" )
    } else {
      .
    }
  } %>%
  add_header_above(plot_header,  align     = "c" )%>%
  kable_styling(position = "center", full_width = TRUE)

```
#### Other Stocks Results {.hidden}
```{r results='asis', eval = any(c("POC", "MAOC")%in%proj.indicators)}
#CHECK! Here's the eval statement if you want to reactive this chunk any(!stocks_df$Stocks_Indicator %in% c("org_c_stocks", "inorg_c_stocks", "all_stocks"))
#Only generates if there is a stocks indicators other than SOC and SIC 
asis_output("### Other carbon pools\\n") #CONDITIONAL HEADER
```
``` {r, results='asis', eval = any(c("POC", "MAOC")%in%proj.indicators)}
#CHECK! Here's the eval statement if you want to reactive this chunk any(!stocks_df$Stocks_Indicator %in% c("org_c_stocks", "inorg_c_stocks", "all_stocks"))
#Only generates if there is a stocks indicators other than SOC and SIC 
asis_output(
#CHECK this sentence sucks
"The following graph shows **all other pools of carbon** measured in this project compared to the sum of all measured carbon pools in the study area."
)
```
```{r, eval = any(c("POC", "MAOC")%in%proj.indicators),fig.cap="Average carbon stocks for all measured pools. Organic carbon stocks are repeated as above to show comparison to other carbon pools measured. The values shown for all measured carbon pools combined is also shown."}
#CHECK! for the eval statement, you'll have to add the acronyms for plant biomass for this to work properly
source("ReportRef/OtherStocksfig_prep.R")
OtherStocksfig
```
#### Texture Results {.hidden}
```{r Texture Explanation, results='asis', eval = "Tx" %in% proj.indicators}
asis_output("### Soil texture\\n") #CONDITIONAL HEADER
```
```{r results='asis'}
if ("Tx" %in% proj.indicators){cat("The graphic below represents where each soil sample falls in a texture triangle, indicating the percentage of clay-sized, silt-sized, and sand-sized particles. The corresponding USDA texture categories are named inside the triangle. In general, we expect clays to store more SOC than sands.")}

```
```{r Texture T and C, results='asis'}
if ("Tx" %in% proj.indicators & length(unique(border$plot_type))==2){cat("Since this project includes a treated and control site, it's also important to note how well the texture values between plots 'match' one another - this helps us interpret differences in carbon dynamics between the sites. Two texture triangles are shown, one for the treated site, and one for the control site.")}

```
```{r texture timepoints, eval="Tx" %in% proj.indicators, results='asis'}
if ("Tx" %in% proj.indicators & (length(unique(PointLevel$timepoint)) > 1)) {
  cat("\\n Note that soil texture is considered an inherent soil property -- we don't expect these values to change due to management intervention. For this reason, texture is generally only measured at the first timepoint to help contextualize results going forward. \\n")
}

```

<br>
```{r, eval="Tx" %in% proj.indicators, results='asis'}
#INSERT TEXT ABOUT TEXTURE T-C MATCH AND AVG SOIL TEXTURE
source('ReportRef/SummaryStats_Tx.R')
cat(tx_avg_sentence)
if(length(unique(PointLevel$plot_type))>1){
  cat(treatment_contrast_siltclay)
}
```

```{r fig.height=6, fig.cap="Texture triangle(s) showing texture classifications for samples in the study area."}
if ("Tx" %in% proj.indicators){ #only do any of this if texture was included as an indicator

  if (length(unique(border$plot_type))==1){ #make one triangle for plots with only T site
    
  texture <- PointLevel %>% 
    rename(SAND=sand, SILT=silt, CLAY=clay) %>%
    filter(!is.na(CLAY)) %>%
    filter(!is.na(SAND)) %>%
    filter(!is.na(SILT))
TT.plot(
  class.sys = "USDA.TT",
  tri.data = texture,
  #z.name = "org_c",
  col="darkblue",
  main = NA,
  grid.show = FALSE,
 class.lab.show="full",
 css.lab = c("Clay %", "Silt %", "Sand %"),
  arrows.show=TRUE,
 cex.axis = 0.8,
 cex.lab = 0.8,
 lwd.lab = 0.8,
# z.col.hue = 0.4,
# z.pch = 1,
 arrows.lty='blank',
 new.mar =c(5.1, 0.1, 0.02, 1) #bottom, left, top, right
) 
  }
  
  if (length(unique(border$plot_type))==2){ #make 2 triangles for plots with a T and a C site
  

    texture_T <- PointLevel %>% 
    rename(SAND=sand, SILT=silt, CLAY=clay) %>%
    filter(!is.na(CLAY)) %>%
    filter(!is.na(SAND)) %>%
    filter(!is.na(SILT)) %>%
    filter(plot_type == "T") #CHECK! This won't work right now until lisa adds this column to PointLevel
    
    texture_C <- PointLevel %>% 
    rename(SAND=sand, SILT=silt, CLAY=clay) %>%
    filter(!is.na(CLAY)) %>%
    filter(!is.na(SAND)) %>%
    filter(!is.na(SILT)) %>%
    filter(plot_type == "C") #CHECK! This won't work right now until lisa adds this column to PointLevel

#this parts funny; in order to get the two plots to show side by side, i have to save them as PNGs first; using a temp directory to keep the working directory clean
    
temp_dir<-tempdir()
TextureT_path <- file.path(temp_dir, "TextureT")
TextureC_path <- file.path(temp_dir, "TextureC")
         
png(TextureT_path)
par(mar = c(0.5, 0.5, 1, 0.5))
TT.plot(
  class.sys = "USDA.TT",
  tri.data = texture_T,
 # z.name = "org_c",
  main = "Treated Site",
    col="darkblue",
  grid.show = FALSE,
 class.lab.show="full",
 css.lab = c("Clay %", "Silt %", "Sand %"),
 arrows.show=TRUE,
 cex.axis = 0.8,
 cex.lab = 0.8,
 lwd.lab = 0.8,
 z.col.hue = 0.4,
 z.pch = 1,
 arrows.lty='blank',
 new.mar = c(.2, 1, .2, 1) #bottom, left, top, right
) 
dev.off()

png(TextureC_path)
par(mar = c(0.5, 0.5, 1, 0.5))
TT.plot(
  class.sys = "USDA.TT",
  tri.data = texture_C,
 # z.name = "org_c",
  main = "Control Site",
  grid.show = FALSE,
     col="darkblue",
 class.lab.show="full",
 css.lab = c("Clay %", "Silt %", "Sand %"),
  arrows.show=TRUE,
 cex.axis = 0.8,
 cex.lab = 0.8,
 lwd.lab = 0.8,
 z.col.hue = 0.4,
 z.pch = 1,
 arrows.lty='blank',
 new.mar = c(.2, 1, .2, 1) #bottom, left, top, right
) 
dev.off()

imgT <- rasterGrob(png::readPNG(TextureT_path), interpolate = TRUE)
imgC <- rasterGrob(png::readPNG(TextureC_path), interpolate = TRUE)

grid.arrange(imgT, imgC, ncol = 2)
  }
  
}
```
### Other measured properties
The following table summarizes average values across the study area for all metrics included in your project. 
```{r} 
#Contains internal conditions for stocks vs non-stocks
source("ReportRef/SummaryTable_prep.R")
SummaryTable
```
### Dynamic maps: point-level results

The previous tables and figures report results averaged across the study area. Use the interactive map below to explore results at each sampling point. Hover over the layers symbol to select from included metrics -- select one layer at a time for best visibility. Click on a sampling point to see all measured values at that point.
```{r, eval=length(unique(PointLevel$timepoint)) > 1, results='asis'}
cat("The first map shows results at the first monitoring timepoint. The second map shows change since the first monitoring timepoint and the last monitoring timepoint.", "\n")

```
```{r, results='asis'}
cat("\n")
cat("#### All measured values in", tp_lookup$year_label[1], "\n")
```
```{r leaflet_maps_firsT, results='asis', echo=FALSE, message=FALSE, warning=FALSE}
source("ReportRef/DynamicMapFirstT_prep.R")
DynamicMap
```

```{r, results='asis', eval=length(unique(PointLevel$timepoint)) > 1}
cat("#### Change from",  tp_lookup$year_label[1], "to", tp_lookup$year_label[length(tp_lookup$year_label)],"\n")
cat("Brighter red points show a greater increase in the toggled indicator. Brigther blue points show a greater decrease in the toggled indicator. White points showed little to no change.","\n")
```
```{r, eval=length(unique(PointLevel$timepoint)) > 1}
source("ReportRef/DynamicMapChange_prep.R")
DynamicMap2
```

This report displays the data and offers no specific interpretations, as management context and land use history also play a role at this site. Contact science staff if further interpretation and conclusions are needed. 

## Frequently Asked Questions

#### Why is monitoring soil organic carbon important?
Soil organic carbon makes up the majority of soil organic matter which provides a variety of services in agro-ecosystems. Soil organic carbon benefits soil organisms by creating a structured, porous environment. In turn, these soil organisms then increase fertility by cycling nutrients from waste materials, leading to higher productivity. Soils with higher levels of organic carbon have higher water holding capacity contributing to overall landscape resilience. Understanding how soil organic carbon changes in response to management practices across a variety of soils and climates, will help us prioritize lands for maximum conservation planting impact.

#### How can I interpret 1 ton of carbon per acre?
One ton of carbon per acre is equal to 3.67 metric tons of CO2 removed from the atmosphere, equivalent to 1 year of passenger car emissions. It's important to note that carbon stored in this report is always related to the sampling depth. While sampling to a 12-inch depth is an industry-standard minimum, much more carbon can be stored below a foot of soil.     

#### As a land steward, how do I use all of this information?
Ag-C data is designed to track changes in carbon storage and cycling over time in response to conservation practices. Over several monitoring cycles, land stewards can use this information to make adaptive management decisions. If time-series data is not yet available, comparing across different areas of your own farm or ranch is most informative. Explore the dynamic map to gauge how different sites across the study area differ in key metrics.    

Additionally, our team is working towards a reporting structure that will allow landowners to compare their Ag-C data to other sites within the network, especially to other enrolled farms and ranches in their region. As our database grows, network-wide and regional comparisons will provide an increasingly detailed understanding of differing practice impacts across spatial scales. An online user dashboard allowing Ag-C land stewards to view and compare their results is planned for launch in 2026. 

If you’d like to pursue options for implementing carbon-building practices, reaching out to your local NRCS office is a great start. You can find a service center in your county at [www.nrcs.usda.gov/contact/find-a-service-center](https://www.nrcs.usda.gov/contact/find-a-service-center). 

#### How can the data from my site contribute to agricultural conservation science?  

Currently, accurate predictions of conservation practice outcomes are limited due to a lack of sufficient data on how on-farm management interacts with local environmental conditions. The Ag-C Program will address this gap by creating a publicly accessible database that spans a broad geographic range and timescale. This resource will enhance our understanding of how farm-level climate and management practices influence conservation outcomes, improve our ability to predict results locally, and support the development of practical, user-friendly tools for producers.  

Participants have the option to contribute their data to the Ag-C Database, where it is securely stored in the Point Blue Science Cloud. Data owners select a sharing level they are comfortable with, ranging from private to open. Please visit [this link](https://form.jotform.com/250767021376053) to read about our data sharing policies and select a data sharing level for your project.  

Detailed management data advances the science of agricultural conservation by helping to tease out relationships between management activities and changes in soil health. If you choose to submit your data to the Ag-C Database, please consider filling out a [Management Questionnaire](https://form.jotform.com/243406539772160) to multiply the impact of your contribution.  

#### How can I continue monitoring my conservation practice over time?
Under the Ag-C framework, each conservation practice has its own recommended resampling interval that ranges from 1 year to 10 years, based on the expected impact of the practice over time^[Consult the [`r proj.protocol` Handbook](`r ifelse(proj.protocol=='Range-C', 'https://www.pointblue.org/?sdm_process_download=1&download_id=19991', 'https://www.pointblue.org/?sdm_process_download=1&download_id=20943')`) for more information about resampling intervals by conservation practice.]. Our team is always working to identify opportunities for continued monitoring for Ag-C participants. Contact our team if you're interested in pursuing support for future resampling events or if you have existing support for future monitoring and would like to continue participating in the Ag-C Monitoring Program.

## Conclusion
Thank you for your participation in the Ag-C Monitoring Program. For more information about project design, further interpretation of results, or to access the raw data from your study site, please reach out to [RangeC\@pointblue.org](mailto:RangeC@pointblue.org) or [CropC\@pointblue.org](mailto:CropC@pointblue.org). Our team is happy to engage with additional questions or feedback. Your participation and sharing data will launch new scientific understanding of conservation management and support improved incentive structures across the nation.